# claude-plugins

Claude Code plugins by Abnormal AI — apply to open roles, explore our engineering culture, and build with our public tools.

## Usage

Add the marketplace:

```
/plugin marketplace add abnormal-ai/claude-plugins
```

Install a plugin:

```
/plugin install apply@abnormal-ai
```

Then use it:

```
/apply
```
